# HuggingFace API Token (OPTIONAL - get from https://huggingface.co/settings/tokens)
# The app works without a token, but adding one improves rate limits and reliability
# HF_API_TOKEN=your_huggingface_api_token_here

# Flask Debug Mode (optional, default: False)
# Set to 'true' for development, 'false' or omit for production
FLASK_DEBUG=false
